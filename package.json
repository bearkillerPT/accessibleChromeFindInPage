{
    "name": "accessible_find_in_page",
    "version": "1.0.0",
    "description": "This extention attempts to solve my issue with finding the position of matching terms in a CTRL+F search. When the user changes the search term in the extension popup the extension will highlight all matching terms and blink alternating between the terms and surrounding words. The number of blinks, blink interval and number of surrounding words can be configured in the extension options.",
    "main": "main.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Gil Teixeira",
    "license": "ISC",
    "dependencies": {
        "@types/chrome": "^0.0.180"
    },
    "devDependencies": {
        "mark.js": "^8.11.1"
    }
}
